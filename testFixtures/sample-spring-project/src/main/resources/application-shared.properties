# Activates for TWO profiles at once, the case PropertiesConfigParser used to collapse to just
# the first (`dev`), silently dropping `stage`. Expect: app.shared.flag shows as SET under BOTH
# dev and stage in the Key Matrix tab, not just dev.
spring.config.activate.on-profile=dev|stage
app.shared.flag=true
